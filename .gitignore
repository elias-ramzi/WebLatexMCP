node_modules
dist/
coverage/
*.log

# Local secrets / config — never commit
.env
.env.local
.mcp.json

# Agent worktrees (nested checkouts of this repo); .claude/settings.json + skills stay tracked
.claude/worktrees/
# Wave/lane specs written for the implementer agents — scratch for one run, not a record of
# the repo; the reasoning that outlives a wave belongs in CHANGELOG.md and the issues.
.claude/plans/

# Editor / OS
.DS_Store
.vscode/
.idea/
tmp/

# Packed Desktop Extension bundle (built on demand / in CI, attached to releases)
*.mcpb
