\documentclass{article}

\title{T}\author{A}
\begin{document}
\section{Intro}
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
\begin{figure}[t]\centering MKfigm1 BODY\caption{Cap MKfigm1}\label{fig:m1}\end{figure}
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
\begin{figure}[t]\centering MKfigm2 BODY\caption{Cap MKfigm2}\label{fig:m2}\end{figure}
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
\clearpage\setcounter{page}{1}\section*{Supplementary}
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
\begin{figure}[t]\centering MKfigs1 BODY\caption{Cap MKfigs1}\label{fig:s1}\end{figure}
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
\begin{figure}[t]\centering MKfigs2 BODY\caption{Cap MKfigs2}\label{fig:s2}\end{figure}
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
\begin{figure}[t]\centering MKfigs3 BODY\caption{Cap MKfigs3}\label{fig:s3}\end{figure}
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
Filler text that stands in for a paragraph of an ordinary paper, long enough to wrap over several lines and fill the page in a handful of repetitions. Prior work reports three runs, and most settings agree.\par
\end{document}
